HTMLWidgets.widget({

    name: 'lovingtimer',

    type: 'output',

    factory: function(el, width, height) {
        var id = el.id;
        return {
            renderValue: function(x) {
            },

            resize: function(width, height) {
            }

        };
    }
});
